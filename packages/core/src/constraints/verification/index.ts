/**
 * Constraint Verification Module
 *
 * Verifies code against architectural constraints.
 */

// Constraint Verifier
export {
  ConstraintVerifier,
  createConstraintVerifier,
} from './constraint-verifier.js';
export type {
  ConstraintVerifierConfig,
  VerifyOptions,
  FileContext,
} from './constraint-verifier.js';
