# Drift V2 ignore patterns
# Uses gitignore syntax — respected by the scan walker automatically.

# ── Drift V1 (legacy, co-located in repo) ──
drift/

# ── Dependencies ──
node_modules/
.pnpm-store/

# ── Build outputs ──
dist/
build/
out/
.next/
.nuxt/
target/

# ── Test files (avoid self-detection noise) ──
# Test fixtures contain intentional secrets, crypto patterns, and magic numbers
# that produce false positives when the analysis tool scans itself.
*_test.rs
*.test.ts
*.test.js
*.spec.ts
*.spec.js
test-fixtures/
tests/

# ── IDE and editor files ──
.idea/
.vscode/
*.swp
*.swo

# ── OS files ──
.DS_Store
Thumbs.db

# ── Logs ──
*.log
logs/

# ── Temporary / generated ──
tmp/
temp/
coverage/
.nyc_output/

# ── Documentation (no code to analyze) ──
docs/
*.md

# ── Docker / CI config ──
docker/
.github/

# ── Lock files ──
package-lock.json
pnpm-lock.yaml
Cargo.lock
